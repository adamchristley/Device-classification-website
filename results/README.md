# Evaluation artifacts

The training workflow generates metrics, per-image predictions, a confusion matrix, and a model card here. Results are committed only after a successful training run on `main`.
